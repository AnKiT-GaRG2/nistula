export function handleAvailability({ firstName }) {
  return `Hi ${firstName}! Great news — the villa is available for those dates. The base rate is INR 18,000 per night for up to 4 guests. Would you like to go ahead and confirm the booking?`;
}
